# Anwar Clinic

## Projects

- **[Landing Page](./landing_page)**: Next.js 14 landing page clone of QHT Clinic (`qhtclinic.com`) with custom component architecture (`components/common/` and `pages/homepage/`).

### Running Landing Page
```bash
cd landing_page
npm install
npm run dev
```
