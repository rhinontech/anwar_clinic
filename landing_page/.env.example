# Base URL of the clinic backend API (see backend/README.md)
NEXT_PUBLIC_API_URL=http://localhost:5050

# Seconds before a statically generated service page refetches its content.
SERVICES_REVALIDATE=60
